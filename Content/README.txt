Placeholder so the source tree carries the Content folder (git cannot track an empty directory,
which would leave Content.meta orphaned on import). The Owlcat modification build emits Content/
into the package; this mod ships no content assets.
